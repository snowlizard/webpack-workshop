import React from 'react';
import ReactDOM from 'react-dom';

const root = document.getElementById('root');
const component = <div>React and webpack are working</div>

ReactDOM.render(component, root);